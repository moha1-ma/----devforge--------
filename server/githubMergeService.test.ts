import { afterEach, describe, expect, it, vi } from "vitest";
import { prepareVerifiedMergePlan, verifyPublicRepository } from "./githubMergeService";
describe("github merge verification", () => { afterEach(() => vi.unstubAllGlobals()); it("verifies only public metadata before returning a reviewed merge plan", async () => { vi.stubGlobal("fetch", vi.fn().mockResolvedValue({ ok: true, status: 200, json: async () => ({ full_name: "acme/ui", html_url: "https://github.com/acme/ui", default_branch: "main", private: false, license: { spdx_id: "MIT" }, language: "TypeScript", updated_at: "2026-08-27T00:00:00Z" }) })); const result = await prepareVerifiedMergePlan({ toolName: "أداة موحدة", repositories: ["acme/ui"] }); expect(result.repositories[0]).toMatchObject({ fullName: "acme/ui", visibility: "public" }); expect(result.sourceDisclosure).toContain("لم تُستنسخ"); }); it("rejects unavailable sources", async () => { vi.stubGlobal("fetch", vi.fn().mockResolvedValue({ ok: false, status: 404 })); await expect(verifyPublicRepository("acme/missing")).rejects.toThrow("غير متاح علنًا"); }); });
