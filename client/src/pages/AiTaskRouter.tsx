import DashboardLayout from "@/components/DashboardLayout";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { useLanguage } from "@/contexts/LanguageContext";
import { AI_ROUTER_MAX_LENGTH, createAiRoutingPlan, normalizeAiTask, vettedAiProviderReferences } from "@/lib/aiTaskRouter";
import { ArrowLeft, Bot, ExternalLink, LockKeyhole, Route, ShieldCheck, Sparkles, Timer, Zap } from "lucide-react";
import React, { useState } from "react";
import { useLocation } from "wouter";

export default function AiTaskRouter() {
  const { direction, t } = useLanguage();
  const [, setLocation] = useLocation();
  const [task, setTask] = useState("");
  const [submitted, setSubmitted] = useState(false);
  const normalized = normalizeAiTask(task);
  const plan = submitted ? createAiRoutingPlan(task) : null;
  const canRoute = normalized.length >= 24;

  return <DashboardLayout><section dir={direction} className="space-y-5 sm:space-y-6"><header className="relative overflow-hidden rounded-[2rem] border border-violet-300/15 bg-gradient-to-l from-violet-400/15 via-slate-950 to-cyan-400/10 p-6 sm:p-8"><div className="absolute -left-10 -top-10 h-40 w-40 rounded-full bg-violet-300/10 blur-3xl" /><Badge className="relative border border-violet-300/20 bg-violet-300/10 text-violet-100"><LockKeyhole className="ml-1 h-3.5 w-3.5" /> {t("routerOwner")}</Badge><div className="relative mt-5 flex gap-4"><div className="grid h-12 w-12 shrink-0 place-items-center rounded-2xl bg-violet-300 text-slate-950"><Bot className="h-6 w-6" /></div><div><h1 className="text-2xl font-bold text-white sm:text-3xl">{t("routerTitle")}</h1><p className="mt-2 max-w-3xl leading-8 text-slate-300">{t("routerIntro")}</p></div></div></header><section className="grid gap-5 xl:grid-cols-[minmax(0,1fr)_320px]"><form onSubmit={(event) => { event.preventDefault(); setSubmitted(true); }} className="rounded-3xl border border-white/8 bg-slate-950/55 p-5 sm:p-6"><div className="flex items-start gap-3"><Zap className="mt-0.5 h-5 w-5 shrink-0 text-cyan-300" /><div><h2 className="font-bold text-white">{t("routerPlannerTitle")}</h2><p className="mt-1 text-sm leading-6 text-slate-500">{t("routerPlannerCopy")}</p></div></div><label htmlFor="ai-router-task" className="sr-only">{t("routerTaskLabel")}</label><textarea id="ai-router-task" value={task} onChange={(event) => { setTask(event.target.value); setSubmitted(false); }} maxLength={AI_ROUTER_MAX_LENGTH} placeholder={t("routerTaskPlaceholder")} className="mt-5 min-h-44 w-full resize-y rounded-2xl border border-white/10 bg-white/[0.035] p-4 leading-7 text-white outline-none placeholder:text-slate-600 focus:border-violet-300/45" /><div className="mt-3 flex flex-wrap items-center justify-between gap-3"><p className="text-xs text-slate-500">{normalized.length}/{AI_ROUTER_MAX_LENGTH} · {t("routerMinLength")}</p><Button type="submit" disabled={!canRoute} className="bg-violet-300 text-slate-950 hover:bg-violet-200"><Route className="ml-2 h-4 w-4" />{t("routerSubmit")}</Button></div>{submitted && !plan ? <p role="alert" className="mt-4 rounded-xl border border-amber-300/20 bg-amber-300/5 p-3 text-sm text-amber-100">{t("routerNeedDetail")}</p> : null}</form><aside className="rounded-3xl border border-cyan-300/15 bg-cyan-300/[0.045] p-5"><div className="flex gap-3"><ShieldCheck className="mt-0.5 h-5 w-5 shrink-0 text-cyan-300" /><div><h2 className="font-bold text-cyan-100">{t("routerSafetyTitle")}</h2><p className="mt-2 text-sm leading-7 text-cyan-100/70">{t("routerSafetyCopy")}</p></div></div></aside></section>{plan ? <section className="space-y-4"><div className="rounded-3xl border border-violet-300/15 bg-violet-300/[0.045] p-5"><p className="text-sm leading-7 text-violet-100">{plan.summary}</p></div><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{plan.workstreams.map((stream) => <article key={stream.id} className="rounded-3xl border border-white/8 bg-white/[0.025] p-5"><div className="flex items-center justify-between gap-3"><Badge variant="outline" className="border-white/10 text-slate-400">{stream.priority}</Badge><Sparkles className="h-4 w-4 text-violet-300" /></div><h2 className="mt-4 font-bold text-white">{stream.title}</h2><p className="mt-2 min-h-12 text-sm leading-6 text-slate-500">{stream.description}</p><Button type="button" variant="outline" onClick={() => setLocation(stream.route)} className="mt-5 w-full border-white/10 text-slate-200 hover:bg-white/8"><ArrowLeft className="ml-2 h-4 w-4" />{t("routerOpenWorkspace")}</Button></article>)}</div><section className="rounded-3xl border border-amber-300/15 bg-amber-300/[0.04] p-5"><div className="flex items-center gap-2"><Timer className="h-4 w-4 text-amber-300" /><h2 className="font-bold text-amber-100">{t("routerOwnerReview")}</h2></div><ul className="mt-3 space-y-2 text-sm leading-7 text-amber-100/75">{plan.boundaries.map((boundary) => <li key={boundary}>• {boundary}</li>)}</ul></section></section> : null}<section className="rounded-3xl border border-white/8 bg-white/[0.025] p-5"><h2 className="font-bold text-white">{t("routerReferences")}</h2><p className="mt-1 text-sm leading-6 text-slate-500">{t("routerReferencesCopy")}</p><div className="mt-4 grid gap-3 sm:grid-cols-3">{vettedAiProviderReferences.map((provider) => <a key={provider.name} href={provider.url} target="_blank" rel="noreferrer" className="rounded-2xl border border-white/8 bg-slate-950/40 p-4 transition hover:border-violet-300/30 hover:bg-violet-300/[0.04]"><p className="flex items-center justify-between gap-2 font-semibold text-white"><span>{provider.name}</span><ExternalLink className="h-4 w-4 text-violet-300" /></p><p className="mt-2 text-xs text-slate-500">{provider.category} · {t("officialDocumentation")}</p></a>)}</div></section></section></DashboardLayout>;
}
