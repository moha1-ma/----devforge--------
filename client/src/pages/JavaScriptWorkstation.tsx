import DashboardLayout from "@/components/DashboardLayout";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { useLanguage } from "@/contexts/LanguageContext";
import { jsSandboxLimits, startJavaScriptSandbox, type SandboxEvent } from "@/lib/jsSandbox";
import { CircleStop, Code2, Eraser, LockKeyhole, Play, ShieldCheck, TerminalSquare } from "lucide-react";
import React, { useEffect, useRef, useState } from "react";

const starter = `// محطة JavaScript محلية ومعزولة\nconst items = [3, 6, 9];\nconst doubled = items.map(value => value * 2);\nconsole.log({ doubled });\n\nreturn doubled.reduce((total, value) => total + value, 0);`;
export default function JavaScriptWorkstation() {
  const { direction } = useLanguage(); const [source, setSource] = useState(starter); const [events, setEvents] = useState<SandboxEvent[]>([]); const [running, setRunning] = useState(false); const control = useRef<{ stop: () => void } | null>(null);
  useEffect(() => () => control.current?.stop(), []);
  function run() { if (running) return; setEvents([]); setRunning(true); try { control.current = startJavaScriptSandbox(source, event => { setEvents(current => [...current, event]); if (event.type !== "log") { setRunning(false); control.current = null; } }); } catch (error) { setEvents([{ type: "error", text: error instanceof Error ? error.message : "تعذر بدء التنفيذ المحلي." }]); setRunning(false); } }
  function stop() { control.current?.stop(); control.current = null; setRunning(false); }
  return <DashboardLayout><main dir={direction} className="space-y-6"><header className="rounded-[2rem] border border-emerald-300/15 bg-gradient-to-l from-emerald-300/10 to-slate-950 p-6 sm:p-8"><div className="flex flex-wrap items-start justify-between gap-4"><div><Badge className="border border-emerald-300/20 bg-emerald-300/10 text-emerald-100"><TerminalSquare className="ml-1 h-3.5 w-3.5" />JavaScript محلي</Badge><h1 className="mt-4 text-2xl font-bold text-white sm:text-3xl">محطة JavaScript المستقلة</h1><p className="mt-2 max-w-3xl leading-8 text-slate-300">نفّذ تجارب JavaScript داخل عامل متصفح معزول. هذه محطة برمجية محلية وليست وكيلًا ذاتيًّا: لا تصل إلى الشبكة أو الحسابات أو الأسرار أو ملفات المنصة.</p></div><div className="rounded-2xl border border-emerald-300/15 bg-slate-950/60 p-3 text-xs leading-6 text-emerald-100"><ShieldCheck className="ml-1 inline h-4 w-4" />مهلة: {jsSandboxLimits.maxRuntimeMs / 1000} ثانيتان<br /><LockKeyhole className="ml-1 inline h-4 w-4" />لا وصول تلقائي للمنصة</div></div></header><div className="grid gap-5 xl:grid-cols-[minmax(0,1fr)_380px]"><section className="overflow-hidden rounded-3xl border border-white/8 bg-slate-950/65"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-white/8 px-5 py-4"><div><h2 className="flex items-center gap-2 font-bold text-white"><Code2 className="h-4 w-4 text-emerald-300" />محرر التجربة</h2><p className="mt-1 text-xs text-slate-500">حد الإدخال {jsSandboxLimits.maxCharacters.toLocaleString("ar")} حرف. لا تحفظ الأكواد داخل المنصة تلقائيًا.</p></div><div className="flex gap-2"><Button onClick={() => { setSource(starter); setEvents([]); }} disabled={running} variant="outline" className="border-white/10 text-slate-300 hover:bg-white/10 hover:text-white"><Eraser className="ml-1 h-4 w-4" />إعادة ضبط</Button>{running ? <Button onClick={stop} className="bg-rose-300 text-slate-950 hover:bg-rose-200"><CircleStop className="ml-1 h-4 w-4" />إيقاف</Button> : <Button onClick={run} disabled={!source.trim()} className="bg-emerald-300 text-slate-950 hover:bg-emerald-200"><Play className="ml-1 h-4 w-4 fill-current" />تشغيل محلي</Button>}</div></div><textarea aria-label="محرر JavaScript" value={source} onChange={event => setSource(event.target.value)} spellCheck={false} dir="ltr" className="min-h-[420px] w-full resize-y bg-transparent p-5 font-mono text-sm leading-7 text-slate-100 outline-none" /></section><aside className="overflow-hidden rounded-3xl border border-white/8 bg-slate-950/65"><div className="border-b border-white/8 px-5 py-4"><h2 className="flex items-center gap-2 font-bold text-white"><TerminalSquare className="h-4 w-4 text-emerald-300" />سجل التنفيذ</h2><p className="mt-1 text-xs text-slate-500">مخرجات العملية المحلية الحالية فقط.</p></div><div className="min-h-[420px] space-y-2 p-4 font-mono text-xs leading-6">{events.length ? events.map((event, index) => <pre key={`${event.type}-${index}`} className={`whitespace-pre-wrap break-words rounded-xl p-3 ${event.type === "error" ? "bg-rose-300/10 text-rose-100" : event.type === "done" ? "bg-emerald-300/10 text-emerald-100" : "bg-white/[0.035] text-slate-300"}`}>[{event.type === "log" ? "سجل" : event.type === "done" ? "اكتمل" : "خطأ"}] {event.text}</pre>) : <div className="grid min-h-80 place-items-center text-center"><p className="max-w-60 text-sm font-sans leading-7 text-slate-500">شغّل تجربة لتظهر المخرجات هنا. ستتوقف الحلقة أو التعليمة الطويلة تلقائيًا بعد المهلة.</p></div>}</div></aside></div><p className="rounded-2xl border border-amber-300/15 bg-amber-300/[0.035] p-4 text-sm leading-7 text-amber-100">هذه المحطة لا تنفذ أوامر نظام، ولا تستخدم مفاتيح API أو ملفاتك الخاصة، ولا تملك وصولًا إلى GitHub أو قاعدة البيانات. احفظ أو انقل النتائج بنفسك بعد المراجعة.</p></main></DashboardLayout>;
}
