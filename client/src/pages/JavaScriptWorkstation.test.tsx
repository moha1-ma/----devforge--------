import React from "react";
import { cleanup, render, screen } from "@testing-library/react";
import { afterEach, describe, expect, it, vi } from "vitest";
import { LanguageProvider } from "@/contexts/LanguageContext";
import JavaScriptWorkstation from "./JavaScriptWorkstation";
vi.mock("@/components/DashboardLayout", () => ({ default: ({ children }: { children: React.ReactNode }) => <div>{children}</div> }));
vi.mock("@/lib/jsSandbox", () => ({ jsSandboxLimits: { maxCharacters: 40_000, maxRuntimeMs: 2_000, maxOutputLines: 120 }, startJavaScriptSandbox: vi.fn(() => ({ stop: vi.fn() })), diagnoseJavaScriptFailure: vi.fn(() => [{ title: "إصلاح للمراجعة", explanation: "اقتراح محلي", suggestedChange: "const value = 0;", boundary: "لا تطبيق تلقائي" }]) }));
describe("JavaScriptWorkstation", () => { afterEach(() => { cleanup(); localStorage.clear(); }); it("presents a controlled local execution station rather than an autonomous agent", () => { render(<LanguageProvider><JavaScriptWorkstation /></LanguageProvider>); expect(screen.getByRole("heading", { name: "محطة JavaScript المستقلة" })).toBeTruthy(); expect(screen.getByRole("button", { name: "تشغيل محلي" })).toBeTruthy(); expect(screen.getByRole("button", { name: "إعادة ضبط" })).toBeTruthy(); expect(screen.getByText(/لا تصل إلى الشبكة أو الحسابات أو الأسرار/)).toBeTruthy(); expect(screen.getByLabelText("محرر JavaScript")).toBeTruthy(); expect(screen.queryByRole("button", { name: "تشخيص الخطأ واقتراح إصلاح" })).toBeNull(); }); it("renders reviewed English controls while keeping the initial source local", () => { localStorage.setItem("devforge-language", "en"); render(<LanguageProvider><JavaScriptWorkstation /></LanguageProvider>); expect(screen.getByRole("heading", { name: "Isolated JavaScript workstation" })).toBeTruthy(); expect(screen.getByRole("button", { name: "Run locally" })).toBeTruthy(); expect((screen.getByLabelText("JavaScript editor") as HTMLTextAreaElement).value).toContain("const items"); expect(screen.getByText(/not an autonomous agent/)).toBeTruthy(); }); });
